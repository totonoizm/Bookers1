<Strong>Editing Book</Strong>

<%= form_with model:@book, local: true do |f| %>
  <h3>Title</h3>
  <%= f.text_field :title %>
  <h3>Body</h3>
  <%= f.text_area :body %>

  <%= f.submit 'Update Book' %>
  <!--<= link_to "Show", @book(book.id) %>-->
  <!--<= link_to "Back", book_new %>-->
<% end %>